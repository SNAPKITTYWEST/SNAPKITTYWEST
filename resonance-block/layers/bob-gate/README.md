# bob-gate — Layer 5 (synthesized)

BOB gate / EVIDENCE|SILENCE evaluator (ten SYNTH constraints). Synthesized in
prior session. Reference implementation exists at `packages/pearl/src/bob-gate.ts`.
Drop sources here; safe floor (0.5).
